{ config, lib, pkgs, modulesPath, ... }:

{
  # Changes to this file will be untracked by git
  # but this file is imported by configuration
  # import your machine specific config files here to have those files tracked
  # but the import of those files not tracked
  imports =
    [  
	
    ];

}
